__pycache__/
.pycache_tmp/
*.py[cod]
.DS_Store

# Local / licensed source exports
ma_export_*.csv

# Large downloaded SEC artifacts and cache
*/documents/
*/_cache/
*/claude_upload_packages/*/selected_docs/

# API outputs can contain full request/response payloads; keep committed examples
# intentional and small.
llm_field_results*.jsonl
llm_field_extractions*.csv

# Secrets — API keys, credentials (NEVER commit; repo is public)
.env
.env.*
*.key

# Python virtual environment
.venv/

# Licensed data (Bloomberg M&A pull, CRSP/WRDS) — keep local, do NOT commit
BBG Data Pull*.csv
US_election_deals_for_analysis.csv
stocknames_cache.csv
cusip_backfill_election/
cusip_manual_overrides.csv

# Backfill run logs
cusip_backfill*.log

# SEC download run dirs (large, regenerable) + CRSP-derived intermediates — keep local
ma_edgar_test/
ma_edgar_run/
ma_edgar_prepare/
ma_field_locator*/
ma_ownership*/
ma_market*/
ma_model*/
target_close_dates.csv
# CIK-resolution audit outputs (regenerable via audit_cik_resolution.py / build_cik_overrides.py)
cik_resolution_audit.csv
cik_override_candidates.csv
ma_edgar_audit_cache/
# NOTE: cik_manual_overrides.csv is hand-verified config (public CIKs) — intentionally NOT ignored; commit it.
partials_4.csv
ma_edgar_partials/
partials_4.csv
ma_edgar_full/

# Run logs (regenerable)
ma_edgar_*.log
*.log
